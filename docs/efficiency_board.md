# M1~M6 六维效率指标结算 — KeyInjector

> **任务编号**：`[D001][78分] 密钥注入器立项`
> **交付版本**：`v1.0.0`　|　**结算日期**：2026-09-22
> **指标定义**：遵循 [`全局规则/memory/efficiency_audit_log.md`](../../全局规则/memory/efficiency_audit_log.md) 六维效率量化审计模型
> **同步记录**：已沉淀至全局效率审计台账「记录 005」

---

## 一、指标结算总表

| 指标 | 名称 | 合格基准 | 本次结算 | 判定 |
| :---: | :--- | :---: | :---: | :---: |
| **M1** | 意图收敛度 (Convergence Rate) | 100% | **100%** | ✅ 达标 |
| **M2** | 首动命名履约率 (Handshake Compliance) | 100% | **100%** | ✅ 达标 |
| **M3** | 工具调用有效率 (Tool Efficiency Ratio) | ≥ 85% | **90%** | ✅ 达标 |
| **M4** | 写后读回覆盖率 (Read-Back Ratio) | 100% | **75%** | ❌ **未达标** |
| **M5** | 流程步骤完整度 (Process Compliance) | 100% | **90%** | ⚠️ 接近 |
| **M6** | 交付入口直达度 (Entrypoint Directness) | 100% | **100%** | ✅ 达标 |

**综合判定**：`[PASS 良好，含 1 项明确未达标]`

---

## 二、逐项依据

### M1 · 意图收敛度：`100%`

首轮即通过三个澄清问题锁定唯一目标，未发生目标漂移：

| 澄清项 | 用户选择 | 收敛结果 |
| :--- | :--- | :--- |
| 注入器语义 | AI API Key 注入器 | 确定为密钥集中保管 + 注入第三方工具配置 |
| 交付形态 | 桌面可视化 + 打包 dmg | 必须产出 `.app` 与 `.dmg`，非仅 CLI |
| 远程仓库 | 由智能体创建 | 独立建仓并强制推送 |

### M2 · 首动命名履约率：`100%`

定标后**首个 bash 调用**即为 `./scripts/rename_session.sh "[D001][78分] 密钥注入器立项"`，
会话标题强一致锁定为规范格式（分类编号 + 难度分 + 8 字概述），无后续标题丢失或截断。

### M3 · 工具调用有效率：`90%`

有效推进调用占比约 `90%`（须扣分的无效调用如下，均属可预防）：

| 无效调用类型 | 次数 | 说明 |
| :--- | :---: | :--- |
| `edit` 未先 `read` 被策略拒绝 | 3 | 违反「先 read 再 edit」铁律，纯浪费 |
| `edit` 因文件 mtime 变更被要求重读 | 2 | `chmod` 改变了观察签名，应先 `chmod` 再编辑 |
| `screencapture` 因 TCC 权限白试 | 2 | 应先做权限探测（见 AAR 建议 E-1） |
| 超长命令被中断，需额外一轮状态核查 | 1 | 长等待应拆分为后台任务，而非单次长阻塞 |
| **合计无效** | **8** | 约占总调用数 10% |

### M4 · 写后读回覆盖率：`75%`　❌ **本次最大短板**

**已 100% 读回验证的（经编译、测试、执行或哈希核对）**：

- `Package.swift` → `swift build` 通过
- `Sources/KeyInjectorApp/*` → 编译通过 + 应用真实启动 + 6 张页面快照渲染正确
- `web/*` → 渲染结果经 6 张截图逐张查看确认（含像素级核对）
- `Sources/KeyInjectorCore/Version.swift`、`Sources/keyinject/CLI.swift` → 编译 + CLI 实测输出
- `scripts/build_app.sh`、`build_dmg.sh` → 真实执行并验证签名与镜像校验
- `scripts/run_tests.sh`、`check_version_sync.sh` → 真实执行并验证退出码
- `.gitignore` → 经远程仓库文件清单核对（`dist/`、`.build/` 确已排除）
- 页面台账中的字节数与 SHA-256 → 经 `stat` / `shasum` 实测复核

**未逐字读回的（本次扣分项）**：

| 文件 | 实际校验方式 | 缺口 |
| :--- | :--- | :--- |
| `README.md` | 版本号 grep + CLI 选项对照实测修正 | 未通读全文核对措辞与结构 |
| `docs/requirements.md` | 版本号 grep 门禁 | 未逐条回读断言与证据链接 |
| `docs/architecture.md` | 版本号 grep 门禁 | 未逐节回读 |
| `skills/keyinject/SKILL.md` | 版本号 grep 门禁 | 未逐条回读命令示例 |
| `.gitattributes` | 无 | 未验证换行符策略实际生效 |

> **诚实结论**：代码与产物侧读回覆盖率为 100%，文档侧明显不足，故综合结算 `75%`。
> 这不是形式主义指标——文档中「命令与实际不符」这类错误**只能靠读回或实测发现**。
> 本次已发现并修正一处（README 中的 CLI 用法经 `--help` 实测对照后修正），说明该风险真实存在。

### M5 · 流程步骤完整度：`90%`

六阶全流程均有实测回执：探境（环境探测）→ 定标（难度评分 78 分 / 价值 51 分 + 标题锁定）→
筹策（架构与安全模型设计）→ 攻坚（核心库 / CLI / 界面 / 打包）→ 质检（38 项测试三轮全绿 +
应用与 DMG 实测 + 版本一致性门禁）→ 归卷（远程硬同步 + AAR + 六维看板）。

扣分项：十六步工序未**逐步**输出显式回执，部分步骤的推进过程未留下可追溯的步骤级记录。

### M6 · 交付入口直达度：`100%`

交付物包含全部直达入口：

- 远程仓库地址（可点击）：`https://github.com/Aki4ever/DSH-KeyInjector`
- 可直接运行的分发产物：`dist/KeyInjector-1.0.0.dmg`、`dist/Key注入器.app`
- 可执行命令：构建、测试、打包、版本门禁四条脚本命令
- 可点击文件路径：需求台账、架构说明、页面台账、AAR、技能包、变更日志
- 结构化复盘卡：AAR 三维报告 + 本六维看板

---

## 三、改进项（下一任务强制改善）

| 编号 | 改进项 | 目标值 | 落地方式 |
| :--- | :--- | :--- | :--- |
| IMP-1 | 纯文档类写后读回 | M4 → 100% | 收尾时对每份文档逐字读回，或按 AAR 建议 B 对文档中每条命令/路径/数字做实测核对 |
| IMP-2 | 技术栈可行性前置探针 | 消除整层返工 | 落地 AAR 建议 E-1 的 `scripts/probe_toolchain.sh` |
| IMP-3 | 无效工具调用压制 | M3 → ≥ 95% | 先 `read` 再 `edit`；先 `chmod` 再编辑；长任务拆后台；动手前做能力探测 |
| IMP-4 | 十六步逐步回执 | M5 → 100% | 每步结束输出一行步骤回执，形成可追溯链 |
